RewriteEngine On
Options -MultiViews

# Forward everything under api/v1 to the matching directory index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ $1/index.php [L]
